import React from 'react';
import { Button } from '@material-ui/core';

const CreateNewExercise = (props) => {
    return (
        <Button variant='outlined' size='small' >Create new exercise</Button>
    );
}
 
export default CreateNewExercise;