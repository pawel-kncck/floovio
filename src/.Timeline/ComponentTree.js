Timeline = {
    TimelineIndex,
    List = {
        ListIndex,
        ListHeader = {
            ListHeaderIndex,
            ExpandIcon,
            ListName,
            NewItemButton,
            ListOptions = {
                ListOptionsIndex,
                OptionsIcon,
                OptionsMenu,
            }, 
        },
        ListBody = {
            ListBodyIndex,
            ListItem = {
                ListItemIndex,
                OpenIcon,
                ItemName,
                FilePath,
                TypeIcon,
                TypeTag,
                StatusContainer,
                PositionButtons,
                ItemOptions,
            }
        },
        ListActions = {
            ListActionsIndex,
            UploadFile,
            CreateNewExercise,
            AddLink
        }
    },
    AddNewList,
}