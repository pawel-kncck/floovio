import React from 'react';
import { Button } from '@material-ui/core';

const UploadFile = (props) => {
    return (
        <Button variant='outlined' size='small' >Upload file</Button>
    );
}
 
export default UploadFile;