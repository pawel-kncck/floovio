import React from 'react';
import { Button } from '@material-ui/core';

const AddLink = (props) => {
    return (
        <Button variant='outlined' size='small' >Add link</Button>
    );
}
 
export default AddLink;